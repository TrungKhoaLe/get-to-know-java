public class LearnVariableScopes {
    public static void main(String[] args) {
        // local = declared inside a method
        // visible only to that method

        // global = declared outside a method,
        // but inside a class and visible to all
        // parts of a class
        //

        LearnVariableScopesDiceRollerClass diceRoller = new LearnVariableScopesDiceRollerClass();
    }
}
